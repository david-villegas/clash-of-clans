.container.pt-5
  %h2{class: 'text-center form-title'} ¿Olvidó su contraseña?
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
    = render 'shared/alert' if (devise_error_messages!.present?)
    .form-group
      .row.justify-content-center
        .col-6
          = f.label :email, 'Correo electrónico', class:'form-font'
          = f.email_field :email, class: 'form-control', autofocus: true, autocomplete: "email"
    .actions.text-center
      .row
        .col
          = f.submit "Reiniciar", class: 'btn mb-2 btn-primary'
    .form-group
      .row.justify-content-center.form-font
        .col-3.text-right
          = link_to "Iniciar Sesión", new_session_path(resource_name)
        .col-3
          = link_to "Registrarse", new_registration_path(resource_name)
%a{href: articles_path}=image_tag 'favicon.ico', class:'btn_home'
